<%= form_for(current_user.active_relationships.build) do |f| %>
  <%= f.hidden_field :followed_id,value: @user.id %>
  <%= f.submit "フォローする", class: "btn btn-outline-primary btn-sm",style: "width:75%; hight:80%; font-size:0.7em;"  %>
<% end %>